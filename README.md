# Godmetatool

Automates Google dorking and parsing results for **authorized OSINT / pentest reconnaissance**.

## Requirements
- Python 3.9+
- Google Chrome / Chromium
- ChromeDriver (compatible version)
- Python deps: `pip install -r requirements.txt`

## Usage
```bash
python3 godmetatool.py -h
``
